
1. JDBC에 대한 설명으로 틀린 것은 무엇입니까?
	- java.sql에서 제공하는 표준 라이브러리를 말한다.
	- DMBS의 종류와 상관없이 사용할 수 잇는 클래스와 인터페이스로 구성되어 있다.
	- JDBC 인터페이스들을 구현한 것이 JDBC Driver이다.
	- JDBC Driver는 DBMS의 종류와 상관없이 동일한 것을 사용할 수 있다. (X)
	
	

2. JDBC가 DB와 연결할 때 필요한 정보가 아닌 것은 무엇인가?
	- DBMS가 설치된 컴퓨터의 IP 주소와 Port 번호가 필요하다.
	- DBMS에 생성된 DB의 이름과 사용자 및 비밀번호가 필요하다
	- DB에 생성된 테이블 이름을 알아야 한다.
	- DBMS별로 제공되는 JDBC Driver 클래스 일므을 알아야 한다
	
	

3. JDBC로 SQL 실행 결과를 얻기 위한 코드 작성 순서는?
	- DriverManager로부터 Connection 얻기
	- Class.forName() 메소드를 이요해서 JDBC Driver 클래스를 로딩하기
	- ResultSet에서 SQL 실행 결과 얻기
	- PreparedStatement를 얻고 SQL문을 실행하기
	

	
4. PreparedStatement에 대한 설명으로 틀린 것은 무엇?
	- 매개변수화된 SQL 문을 사용할 수 있다.
	- INSERT, UPDATE, DELETE 문은 executeUpdate() 메소드로 실행한다
	- SELECT 문은 executeQuery() 메소드로 실행한다.
	- 매개변수화된 SQL 문의 ? 순번은 0번부터 시작한다.
	
	
	
5. ResultSet에 대한 설명으로 틀린 것은 무엇?
	- ResultSet은 executeQuery의 리턴값이다.
	- next() 메소드로 afterLast로 이동할 떄 true를 리턴한다
	- ResultSet은 한 번에 하나의 행만 읽을 수 있다
	- ResultSet은 다음 행으로 커서를 이동할 때 next() 메소드를 사용한다
	
	

6. 프로시저와 함수를 실행하는 방법으로 틀린 것은?
	- CallableStatement를 이용한다
	- 프로시저 호출 문자열로 "{ call 프로시저명(?,?,...)}" 를 사용한다
	- 함수 호출 문자열로 " ? = {call 함수명(?, ?, ..)}" 를 사용한다
	- 리턴값인 ?를 지정할 때에는 registerOutParameter() 메소드를 이용한다
	
	


7. 트랜잭션에 대한 설명으로 틀린 것은?
	- 기능 처리 최소 단위를 말한다
	- 커밋(Commit)은 내부 작업을 모두 성공 처리한다
	- 롤백(Rollback)은 내부 작업 중에서 성공한 작업까지 되돌린다
	- 트랜잭션을 코드로 제어하려면 setAutoCommit(flase) 메소드를 먼저 호출해야 한다
















	